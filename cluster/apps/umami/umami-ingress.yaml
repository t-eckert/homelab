apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: umami
  namespace: umami
  annotations:
    tailscale.com/funnel: "true"
spec:
  defaultBackend:
    service:
      name: umami
      port:
        number: 3000
  ingressClassName: tailscale
  tls:
    - hosts:
      - umami
