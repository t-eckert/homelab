apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: workbench-code
  namespace: workbench
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi

