apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: workbench-data
  namespace: workbench
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 50Gi

