apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - cloudflared-deployment.yaml
  - network-policy.yaml
  - image-repository.yaml
  - image-policy.yaml

# Override the image at apply-time without modifying deployment.yaml in Git
images:
  - name: ghcr.io/t-eckert/field-theories
    newTag: 20251222-0428-sha-82755cd  # Update this manually when needed
